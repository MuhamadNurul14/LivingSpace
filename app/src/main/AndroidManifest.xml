<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LivingSpace"
        tools:targetApi="31">

        <!-- Splash Screen Activity -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.LivingSpace.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Activity -->
        <activity
            android:name=".activities.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Register Activity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Main Activity (Home/Dashboard) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Search Activity -->
        <activity
            android:name=".activities.SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Favorite Activity -->
        <activity
            android:name=".activities.FavoriteActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- History Activity -->
        <activity
            android:name=".activities.HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Profile Activity -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Detail Kosan Activity -->
        <activity
            android:name=".activities.DetailKosanActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Booking Activity -->
        <activity
            android:name=".activities.BookingActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Payment Activity -->
        <activity
            android:name=".activities.PaymentActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

        <!-- Filter Activity -->
        <activity
            android:name=".activities.FilterActivity"
            android:exported="false"
            android:theme="@style/Theme.LivingSpace.NoActionBar" />

    </application>

</manifest>
